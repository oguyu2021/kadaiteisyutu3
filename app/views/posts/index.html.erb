<h1>ポスト一覧</h1>

<table>
  <tr>
    <th>画像</th>
    <th>内容</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.image %></td>
    <td><%= post.content %></td>

    <td><%= link_to '詳細を確認する', post_path(post.id) %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to '新しくポストを投稿する', new_post_path %>